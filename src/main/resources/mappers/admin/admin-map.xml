<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nuguna.freview.admin.mapper.AdminMapper">

  <!-- resultMap 설정 -->
  <!-- adminMap 설정 -->
  <resultMap id="adminMap" type="com.nuguna.freview.admin.vo.AdminVO">
    <id property="seq" column="seq"/>
    <result property="code" column="code"/>
    <result property="email" column="email"/>
    <result property="password" column="password"/>
    <result property="nickname" column="nickname"/>
    <result property="profilePhotoUrl" column="profile_photo_url"/>
    <result property="introduce" column="introduce"/>
    <result property="subEmail" column="sub_email"/>
    <result property="createdAt" column="created_at"/>
    <result property="updatedAt" column="updated_at"/>
  </resultMap>

  <!-- customerManageMap 설정 -->
  <resultMap id="customerManageMap" type="com.nuguna.freview.admin.dto.response.CustomerListDTO">
    <id property="seq" column="seq"/>
    <result property="code" column="code"/>
    <result property="nickname" column="nickname"/>
    <result property="email" column="email"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <!-- SQL 쿼리 -->
  <!-- 관리자 정보 조회 -->
  <select id="selectAdmin" parameterType="Long" resultMap="adminMap">
    select
      seq, code, email, password, nickname, profile_photo_url, sub_email, created_at, updated_at
    from user
    where seq = #{userSeq}
  </select>

  <!-- 회원 목록 조회 -->
  <select id="selectCustomerList" parameterType="com.nuguna.freview.admin.dto.request.CustomerListRequestDTO" resultMap="customerManageMap">
    SELECT
      seq, code, nickname, email, created_at
    FROM user
    WHERE code = 'CUSTOMER'
      AND seq &lt; #{previousUserSeq}
    ORDER BY seq DESC
      LIMIT #{limit}
  </select>

  <!-- 일치하는 관리자 정보 조회 -->
  <select id="selectMatchingAdmin" parameterType="map" resultType="int">
    select
        count(seq)
    from user
    where seq = #{adminSeq} and password = #{password}
  </select>

  <!-- 유저 삭제 -->
  <delete id="deleteUser" parameterType="Long">
    delete
    from user
    where seq = #{userSeq}
  </delete>

</mapper>
